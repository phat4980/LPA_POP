# LPA POP HTTP Contracts

This directory will contain the versioned contract between the Node automation service and the existing Python Web 2.

`openapi.yaml` is intentionally added before the first cross-runtime integration, not as an empty placeholder during the scaffold phase.
